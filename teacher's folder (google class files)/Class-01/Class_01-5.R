# Исходные данные
x1 = 100
y1 = -50
x2 = 500
y2 = 1000

dx = x2 - x1
dy = y2 - y1

math_az = 180 * atan2(dx, dy) / pi # Математический азимут

geo_az = 0 # Географический азимут

if (az >= 0 && az <= 90) {
  geo_az = 90 - math_az
} else {
  geo_az = 450 - math_az
}

# Выведем на экран, округлив азимут до 0.1 градуса
cat('Географический азимут с точки (', x1, ', ', y1, ') на точку (', x2, ', ', y2, ') равен ', round(geo_az, 1), '°', sep = '')
